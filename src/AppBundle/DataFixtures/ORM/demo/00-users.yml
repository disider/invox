AppBundle\Entity\User:
    owner:
        email: demo.owner@invox.it
        plainPassword: demo
        enabled: true
        roles: [ROLE_OWNER]
    manager:
        email: demo.manager@invox.it
        plainPassword: demo
        enabled: true
        roles: {  }
    accountant:
        email: demo.accountant@invox.it
        plainPassword: demo
        enabled: true
        roles: {  }
    salesAgent:
        email: demo.sales@invox.it
        plainPassword: demo
        enabled: true
        roles: {  }
    superadmin:
        email: demo.superadmin@invox.it
        plainPassword: superadmindemo
        enabled: true
        roles: [ ROLE_SUPER_ADMIN, ROLE_ALLOWED_TO_SWITCH ]